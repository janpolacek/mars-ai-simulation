# Astro and Node artifacts
**/node_modules/
**/dist/
**/.astro/

# Generated website copies of root shared assets
website/public/assets/*

# Local media generation and review artifacts
media-work/
comfyui-runs/

# In-flight agent worktrees (one checkout per running kanban card, up to a few GB:
# never staged, never committed, and kept out of `git stash -u`/`git add -A`)
.worktrees/

# Editor and OS files
.DS_Store

# Retired task tracker (Beads dropped; history in docs/beads-archive.jsonl)
.beads/
