# 010 — First surface checks

RH-01 commissions its systems, returns a first rover point-of-view image, and
performs initial movement tests. The early defect becomes observable but does not
prevent basic operations.

**Dependency:** 009.
