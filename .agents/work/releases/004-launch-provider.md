# Release record: 004-launch-provider

## Status

**`released` — the `publication` flip and the assigned record date were applied to
`website/news/004-launch-provider.mdx` in one change by card `t_de6e9055` (commit `4d1795a`), pushed
as `ad8f5ac..4d1795a`, and the article route is live on the production origin.**

The release decision itself was recorded **before** that push, on the editorial gate card
`t_35061463` (`.agents/work/reviews/004-launch-provider.md` §12.5), because `AGENTS.md` requires the
release decision to exist before a push carries content into the public site. This card carries that
decision forward, fixes the exact public scope, applies the four constraints the step-004 gate ledger
routed to it, and **verifies the live surface** rather than re-deriving the decision.

**Authority note, because this card's body reads as a release stage the merged role discharges.** The
operator instruction of 2026-09-17 merged the _Copy editor_ and _Continuity redactor_ roles into
`mars-ai-simulator-editor` and made canon confirmation and releasability that role's own decision,
with its recorded decision being the approval — no human sentence is required for an article's
releasability. Gates 004-A … 004-E below are quoted from the ledger card `t_4d1b3041` with the
answers the human story owner recorded there; the **release decision is this role's own sentence**,
recorded on `t_35061463` (§12.5) and restated in §"The release decision" below. The gate's discharge
was a gate-ledger verdict, not a release (§R3.8, C9).

**This card applied nothing and verified by reading only.** No `website/` file, article byte,
frontmatter field, asset, canon file or `docs/` file was changed by this card; no build was run
(`website/scripts/check-dist.mjs` **prunes** offending files from `dist/` when it fails, so a release
card must never build over another card's output); no commit, no push, no deploy action and no
external service was touched. This card's only write is this record.

---

## The release decision

> **Release decision, recorded 2026-09-17 CEST by `mars-ai-simulator-editor`, carried forward here
> from the editorial gate card `t_35061463` (review record §12.5): the package
> `website/news/004-launch-provider.mdx` at sha256
> `6d79c6e2f0fb6b9bb186e3b8244ef6055c4eca0b2960902354f8a6251fe14080` (3,999 B, `publication: draft`)
> IS RELEASED.** Canon is confirmed against released material, continuity is clear
> (`.agents/work/continuity/004-launch-provider.md`, `continuity clear`), the editorial gate is
> `approved`, no unresolved material failure remains, and the article may go public. The build card
> `t_de6e9055` applies the `publication: published` flip **and** `simulatedDate: 2030-03-11` in one
> change and pushes; this card records the live verification and the exact public scope.

Any change to the article's bytes voids the decision and returns the package to the editorial gate.
The article bytes did not change between the decision and this verification (§"Released hashes").

### Exact public scope

| Surface                                | Content                                                                                                                                                                                                                                                                                                                                                                  |
| -------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Route                                  | `/news/004-launch-provider/` — one page, generated by `src/pages/news/[slug].astro` for a published item only. Live **200**, 9,730 B, sha256 `49e0227d943befaaad21db70f7d97c33b1899cfac1ce0acb3ba4dbd158743dea`                                                                                                                                                          |
| Card on `/` and `/news/`               | the fourth card in frontmatter order: kicker `Launch architecture 04 · Architecture selected`, the writer's drafted title `The launch architecture, chosen.`, the 153-character summary, `Simulated record date · 11 March 2030`, link label `Launch architecture briefing`, amber placeholder cell (`news-placeholder placeholder-amber`, number `04`) and **no image** |
| Frontmatter metadata                   | `publication: published` (the flip) + `simulatedDate: 2030-03-11` (this role's value), on top of the existing `title`, `category`, `status`, `summary`, `linkLabel`, `order: 4`, `accent: amber`. No `author`, no `media*`                                                                                                                                               |
| Plate and emitted derivative           | **None** — no media key, no approved asset, no `docs/` file and no `/_astro/` derivative enter this release; the article page renders no `<figure>` and no article image, and only the site logo is emitted for it                                                                                                                                                       |
| Provider / vehicle designation strings | `Ariane 64` — the launch-vehicle designation Gate 004-A cleared for public copy; 5 occurrences on the article page, 1 in the summary as it renders on `/` and `/news/`. **No** real institution, operator or facility string, anywhere                                                                                                                                   |

---

## Gate ledger

Gates 004-A … 004-E are quoted from the ledger card `t_4d1b3041` (its body carries the required
wordings; its comment thread carries the answers the human story owner recorded through the operator
at 18:52 CEST on 2026-09-17). `.agents/work/reviews/004-launch-provider-gates.md` §R3.1–§R3.3 holds
the full quoting and the fidelity per gate; §R3.2 records that 004-A and 004-C are verbatim, 004-E
not applicable, 004-B arrived in the card body's second ("not required") form, and 004-D is a
recorded deferral rather than either template form.

### Gate 004-A — launch-provider canon

**Required verbatim (the card body's first form):**

> "004 launch provider canon: option A — the locked canon stands (Ariane 64 from Europe's Spaceport near Kourou); the launch vehicle documented is Ariane 64."

**Recorded answer (human story owner, verbatim):** the sentence above, character for character.
Options B and C are not selected; no fictional provider and no fictional vehicle is introduced.

**Applied scope:** the launch **vehicle designation** the locked canon already carries
(`docs/SCENARIO.md:22`, `:124`, `:193`, `:230`) is cleared for public copy — naming it is canon, not
new canon — and it is the only real-world name this release publishes. **Fail-closed default** (the
measured state until the flip): no provider or vehicle name in public copy, no launcher dossier
published, no launcher imagery, no wiki vehicle node, article `publication: draft`, no flip.

### Gate 004-B — real-provider wording review

**Required verbatim (the card body's approval form):**

> "004 real-provider wording reviewed and approved: <the exact public sentence(s)>"

**Recorded answer (the card body's second form, verbatim):** "004 real-provider wording review: not
required — no real provider or institution named", carried with the operator's two constraints: **no
institution name on any public surface** (article, dossier, wiki page, asset manifest, alt text,
captions, slug, SEO package, commit message), and **004-B re-opens** — needing the approval form above,
covering the exact sentence — before any surface names one.

**Applied scope, reconciled in favour of the fail-closed default.** No approval sentence exists, so
this answer clears **nothing** that names a real institution, provider, operator or facility; it is
the form whose premise is that no such name is used. Only the vehicle designation of 004-A is cleared.
The six accurate / non-endorsing rules still bind every sentence they cover, and this role applies them
at release time — §"The six accurate / non-endorsing rules" below is that review (ledger C2).
**Fail-closed default:** the institution/operator/facility name appears in no public surface; measured
0 hits on the released article and on every live route (§"Withheld-marker scan").

### Gate 004-C — release-order advance to step 004, and the launch-vehicle wiki node's publication state

**Required verbatim (the card body's positive form):**

> "Step 004 release order advance: approved" — and — "the launch-vehicle wiki node IS public at step 004 in publication STATE."

**Recorded answer (human story owner, verbatim):** both clauses above.

**Applied scope:** the release order advances to step 004 (`docs/SCENARIO.md:279`), which is what
allows this article's flip and the dossier's existence. The **publication FLIP** for the wiki node is
_not_ granted by this gate: it stays this role's recorded release decision on that page's own release
card. Measured state at this verdict: `/wiki/vehicle/`, `/wiki/vehicle/rh-01-pathfinder/`,
`/wiki/area/` and `/wiki/project/` all **404** — the node is not public, so the state grant has not
been turned into a flip. **Fail-closed default:** step 004 unreleased; article `draft`; every wiki
vehicle route 404.

### Gate 004-D — public title and byline

**Required verbatim (the card body):**

> "004 public title: <title>" · "004 author: <name or 'anonymous'>"

**Recorded answer (a deferral, in the human story owner's wording):** "004 public title: to be set at
the release gate; the fail-closed default (the writer's drafted title) applies until then." · "004
author: none (no byline)."

**Applied scope (ledger C10):** **no byline was added anywhere, and this card set or changed no
title.** The published title is the writer's drafted title, `The launch architecture, chosen.`
(32 characters; document title `The launch architecture, chosen. | Red Horizon`); the content schema
(`website/src/content.config.ts`) declares no `author` field and none was added. Neither may change
without a further recorded human answer. **Fail-closed default:** the drafted title stands; no byline.

### Gate 004-E — `docs/SCENARIO.md` amendment list

**Required only if Option B or C.** Recorded answer: "Not applicable: Option A is selected, so
`docs/SCENARIO.md` stays as it is and no amendment is requested. No planner edit to canon."

**Applied scope:** `docs/SCENARIO.md` is unchanged by this release; no canon was authored by this
role at any point in the step. **Fail-closed default:** canon unchanged.

---

## Containment checklist — resolved line by line

Copied verbatim from the card body. `[x]` below means _measured on the released bytes and on the live
surface in this run_, not assumed from a brief.

- [x] No launch day, time, or date beyond the 2031 launch window already released by article 001.
- [x] No statement that the vehicle is in transit, integrated at the pad, or flight-ready.
- [x] No landing site, no Mars surface imagery, no Asteria Field material beyond what article 001 released. Landing-design facts are withheld (`docs/SCENARIO.md:279`).
- [x] No performance, mass, cost, or specification figure that is not approved canon.
- [x] No real institution or provider named without Gate 004-B's verbatim approval.
- [x] No step 005–011 fact, no private-calendar fact, no unreleased detail from `docs/timeline/`.
- [x] Withheld markers (the two landing-region coordinate markers; `docs/timeline/` paths; `docs/vehicle/contact-arm-scene.png` if the dossier is RH-01-derived; the gated name stems) — scanned over the live route, **0 hits**.

| # | Item                                                                    | Resolution                                                                                                                                                                                                                                                                                                                                                                                                       |
| - | ----------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 | No launch day, time or date beyond the 2031 window                      | **Holds.** The article states the 2031 launch window and that "no day within it has been announced"; no calendar date appears in the article, the frontmatter, the summary or the built page. The one date on the rendered surface is the article's own in-fiction record date (2030-03-11, §"The simulated record date"). Withheld launch-day forms: **0 hits** in the released bytes (§"Withheld-marker scan") |
| 2 | No transit / at-the-pad / flight-ready statement                        | **Holds.** The article's own closing frame is "integration and readiness work … establishing that the mission is ready to fly", and it says explicitly that no part of the flight stack has been integrated with a launcher or delivered to a launch site. The frontmatter `status` is `Architecture selected`, a design state                                                                                   |
| 3 | No landing site, no Mars surface imagery, no Asteria Field beyond 001   | **Holds.** 0 hits for landing-region, ellipse, science-zone, hazard, traverse or `AF-*` tokens; the article carries no media key, no figure, no caption and no alt text, so no imagery can carry one. `docs/SCENARIO.md:279` keeps landing-design facts withheld and none is published by step 004                                                                                                               |
| 4 | No performance, mass, cost or specification figure                      | **Holds.** No figure of any kind is printed: the mass ceiling, the planning allowance and the programme frame are all deliberately unstated. 0 numerical-specification hits                                                                                                                                                                                                                                      |
| 5 | No real institution or provider without 004-B's verbatim approval       | **Holds.** 004-B was answered in its "not required" form and no approval sentence exists, so nothing naming one may ship; measured **0 hits** for the operating-company, agency and facility families on the released article and on every live route checked. The single real string is the designation 004-A cleared                                                                                           |
| 6 | No step 005–011 fact, no private-calendar fact, no `docs/timeline/` end | **Holds.** 0 hits for `docs/timeline` paths, 0 for the withheld marker list, and the article states no launch day, no cruise-phase date and no later-step event                                                                                                                                                                                                                                                  |
| 7 | Withheld markers: 0 hits on the live route                              | **Holds — measured.** §"Withheld-marker scan" reports 0 hits for the two landing-region coordinate markers, the withheld timeline path family, the withheld file name and the institution/operator/facility and launch-day families across `/news/004-launch-provider/`, `/news/`, `/`, 001, 002, 003, `/wiki/`, `/about/` and the 404 probes                                                                    |

---

## Media key decision

**No media key. No plate. No emitted derivative.** The released frontmatter declares no `media*` field
(checked by parsing the released bytes, §"Released hashes and identity"), so `newsMediaIssues()`
(`website/src/lib/media.ts`) has nothing to require, and the article document is not a media consumer
at all.

`newsMediaKeys` at this verdict is the four published keys — `programme-identity`, `asteria-plates`,
`payload-sensor-illustration`, `vehicle-references` — and **no launcher key was added by the visuals
card or the build card**. Measured consequence: the article page renders 0 `<figure>` and no article
image; the newsroom and homepage cards for 004 render the amber placeholder
(`news-placeholder placeholder-amber`) numbered `04`; and the only images emitted for these routes are
the site logo plus the three plates the published 001–003 items already carry. The visuals candidate
for a launcher plate (`cb09992c…`) stays a candidate and unplaced; its selection is the human story
owner's and would need its own media key and its own release decision.

---

## The release change applied (by card `t_de6e9055`, not by this card)

`website/news/004-launch-provider.mdx` — **frontmatter only; the body bytes are unchanged.**

```diff
 title: The launch architecture, chosen.
 category: Launch architecture 04
 status: Architecture selected
-publication: draft
+publication: published
+simulatedDate: 2030-03-11
 summary: "Red Horizon One's delivery architecture is decided: an Ariane 64 launcher, a cruise stage for the transfer to Mars, and the integration work still ahead."
 linkLabel: Launch architecture briefing
 order: 4
 accent: amber
```

In git terms the path was **untracked** in the shared checkout before the build card staged it, so
`4d1795a` records the whole file as a new path plus the two release lines; the release-relevant delta
against the approved revision is exactly those two frontmatter lines. The same commit carried one
test repair the release exposed (`website/test/news-plate-fill.test.mjs`: the first published item
with no media key renders a class-list placeholder cell the suite's matcher could not see — red-first
proven by the build card, no assertion loosened). This release card ran no build and wrote no
`website/` path.

**The flip diff, re-derived by this card from the released bytes** (script outside the repository,
`/tmp/rh-004-release/verify-flip.py`): removing the `simulatedDate` line and reverting
`publication: published` to `draft` in the released file reproduces the approved revision exactly —
sha256 `6d79c6e2f0fb6b9bb186e3b8244ef6055c4eca0b2960902354f8a6251fe14080`, 3,999 B — so the published
file differs from the approved one by those two lines and nothing else (3,999 B → 4,029 B, +30 B:
the inserted `simulatedDate: 2030-03-11` line and its newline, plus `draft` → `published`).

## Released hashes and identity

| Item                                                               | Value                                                                                                                                                                                   |
| ------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Article, approved revision (review §12.5)                          | `6d79c6e2f0fb6b9bb186e3b8244ef6055c4eca0b2960902354f8a6251fe14080` — 3,999 B, 57 lines, `publication: draft`                                                                            |
| **Article, released revision (the flip)**                          | **`7da71fec1f2ce0dbaff8aa2966e802b6de439eb78b73e34b1a8eafab5d764ca3`** — 4,029 B, 58 lines, `publication: published`, `simulatedDate: 2030-03-11`                                       |
| Released body                                                      | unchanged by the flip: 3,637 characters, 578 words, single `h1`, no `h2`, three internal links                                                                                          |
| Frontmatter field check (parsed with the pipeline's own `js-yaml`) | keys: `title`, `category`, `status`, `publication`, `simulatedDate`, `summary`, `linkLabel`, `order`, `accent`; no `author`; no `media*`; `summary` 153 characters; title 32 characters |
| Live article page                                                  | `/news/004-launch-provider/` — 200, 9,730 B, sha256 `49e0227d943befaaad21db70f7d97c33b1899cfac1ce0acb3ba4dbd158743dea` — byte-identical to the build card's own build output            |
| Newsroom index / homepage                                          | `/news/` 200, 12,410 B, `eac56e1e0e8935f181f6c4c0c5598c7ff5d03e7abd35f4fb0be8178c32958189` · `/` 200, 18,846 B, `4c16d7f103e4bf07068fd19b39f02ca4d59bb43ebd17ef92b5e5b5ec576f667a`      |
| Commits                                                            | `4d1795a` (the release change: article + the plate-fill test repair), pushed `ad8f5ac..4d1795a`; `HEAD` = `origin/main` = `4d1795a`, `rev-list origin/main..HEAD` = 0                   |
| Build / deploy evidence (the build card's own)                     | CI workflow "repository gates" run `35254536487` **success**; Cloudflare "Workers Builds" **success**; 12 live routes/assets byte-identical to that build                               |
| Release decision recorded                                          | card `t_35061463`, `.agents/work/reviews/004-launch-provider.md` §12.5 (19:33–19:52 CEST)                                                                                               |
| Released and verified by                                           | `mars-ai-simulator-editor`, gate `t_35061463` (decision) and card `t_1105aa0d` (this record, 19:47–20:0x CEST)                                                                          |
| Guard module at verdict time                                       | `website/scripts/guards.mjs` — sha256 `3c95f22386a0e322d696aa976701ca5de1e0c28828e14cde085c70605c2a358c`, unchanged by the release                                                      |

**This card's own write:** `.agents/work/releases/004-launch-provider.md`, sha256 recorded on card
`t_1105aa0d` (self-embedding is impossible).

---

## The simulated record date — `2030-03-11`, milestone line `docs/SCENARIO.md:188`

> `simulatedDate: 2030-03-11` — milestone line **`docs/SCENARIO.md:188`**,
> "11 March 2030 — RH-01 flight-model assembly begins".

This value is this role's alone to set and verify. It was assigned and verified by the editorial gate
`t_35061463` (review record §6 and §12.4) and re-verified on the live surface by this card.

| Check                                                          | Result                                                                                                                                                                                                                                                                                                                                                   |
| -------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Drawn from a locked milestone the table actually holds         | Yes — `docs/SCENARIO.md:188`, in the "Launch and mission dates" table (`:173`)                                                                                                                                                                                                                                                                           |
| Step correspondence                                            | step 004 releases the launch-architecture decision; `:188` is the first row after step 003's anchor (`:187`, the critical design review / design freeze, carried by article 003 as 2029-07-13) and the only row in the window `:188`–`:192` whose state matches what the article claims — assembly, integration, tests and readiness reviews still ahead |
| Rows excluded, and why                                         | `:189`–`:192` each name an event that would put the article's own "what remains before flight" framing in the past (a test campaign under way, qualification complete, the stack at the launch site, a readiness review held); `:193` is step 005's launch row and is withheld                                                                           |
| Implies no unreleased event or withheld mission date           | Yes — flight-model assembly is not a launch, landing or mission date, and the article states no launch day                                                                                                                                                                                                                                               |
| Contradicts no published sentence                              | No — articles 001–003 carry 2026-10-12, 2027-03-19 and 2029-07-13, all earlier, so the published items stay in timeline order by the dates they carry as well as by `order`                                                                                                                                                                              |
| Rendered surface states it, under an in-fiction label          | **Yes, measured live.** The article page renders `Simulated record date · 11 March 2030` exactly once (`.article-date`), and the `/` and `/news/` cards render it once each. The label is owned by `src/lib/simulated-date.ts` (`simulatedDateLabel = 'Simulated record date'`, separator U+00B7)                                                        |
| Never presented as a real publication date or machine-readable | **Yes, measured live.** 0 ISO-format tokens on the page, 0 `<time>` elements, no `datePublished`/`dateModified`/Open-Graph date meta, no sitemap value (`/sitemap.xml` is 404 site-wide, pre-existing). The page head carries only the summary as `description`, the title and the stylesheet link                                                       |
| Order floor handed forward                                     | It is a **lower bound on step 005's value**: step 005 must carry a record date later than 2030-03-11 (§"Conditions", row 3)                                                                                                                                                                                                                              |

## The six accurate / non-endorsing rules, applied to the exact public sentences (ledger C2)

The gate ledger routed this review to the release decision (C2; §R3.3). Scope: the sentences that name
the launch vehicle and describe the campaign. Quoted from the released bytes, which are the live page's
source.

| Rule (canon proposal §"Accurate / non-endorsing wording rules")                        | Verdict                                                                                                                                                                                                                                                                                                                                                                           |
| -------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1. The fiction is labelled as fiction on every public surface                          | **PASS** — the article's first sentence: "Red Horizon is a fictional, AI-assisted Mars exploration programme"; the closing note repeats it; the site chrome and the article's own link to `/#mission` carry the same framing                                                                                                                                                      |
| 2. No implication of participation, approval, funding or endorsement                   | **PASS** — "Its record belongs to the organisations that build and operate it, not to Red Horizon, and no real agency, launcher operator, or launch facility is a partner in this programme or has endorsed it", and again in the closing note: "no real agency, launcher operator, or launch facility is a partner in Red Horizon, and none of them has endorsed or approved it" |
| 3. No real price, cost, schedule or performance figure presented as the programme's    | **PASS** — no figure of any kind is printed; the launch-service planning allowance is never stated as a price or a commitment                                                                                                                                                                                                                                                     |
| 4. Nothing reads as an advertisement, endorsement or sales pitch for the real provider | **PASS** — the vehicle is stated as an architecture fact and its record explicitly "not to Red Horizon"; no superlative, no performance claim, no supplier attribution                                                                                                                                                                                                            |
| 5. The real provider's public record is separated from the programme's interface       | **PASS** — paragraph 2 states the vehicle and its record; paragraphs 3–5 describe only the programme's own interface (the injection and payload-adapter segment), the cruise stage, the lander platform's EDL and the relay architecture                                                                                                                                          |
| 6. Verbatim wording review by the human before publication                             | **Not required in this form** — 004-B returned the "not required — no real provider or institution named" answer, so there is no real-named sentence to approve; the rules above are this role's applied review, recorded here as the ledger requires                                                                                                                             |

The sentences the rules govern, as released: the designation paragraph ("The launch vehicle is Ariane
64, the four-booster variant of the Ariane 6 launcher family. Ariane 64 is a real vehicle; naming it
here says which launcher this fictional mission is planned around, and nothing more …"), the campaign
paragraph ("The schedule frame remains the 2031 launch window set out in the programme announcement,
and no day within it has been announced. What sits between this decision and the flight is integration
and readiness work … The programme has published a decision, not a countdown.") and the closing note.
No sentence names an institution, operator or facility, so the fail-closed 004-B default holds.

---

## Verification performed by this card — read-only, measured

Everything below was read, hashed or fetched in a real browser on the live origin; nothing was
written into `website/` and no build was run. The probe scripts live outside the repository
(`/tmp/rh-004-release/`). Live measurements: 2026-09-17 ~19:47–19:52 CEST, origin
`https://mars-ai-simulation.janpolacek.workers.dev`, in-page `fetch(path, {cache: 'no-store'})` with
`crypto.subtle.digest('SHA-256', …)` over `arrayBuffer()`.

### 1. The live origin — the surface the push deploys

| URL                                                                                  | Status  | Measured                                                                               |
| ------------------------------------------------------------------------------------ | ------- | -------------------------------------------------------------------------------------- |
| `/news/004-launch-provider/`                                                         | **200** | 9,730 B, `49e0227d…` — the released article page                                       |
| `/news/`                                                                             | 200     | 12,410 B, `eac56e1e…` — 4 cards, all four links                                        |
| `/`                                                                                  | 200     | 18,846 B, `4c16d7f1…` — 4 cards, all four links                                        |
| `/news/001-project-announcement/`                                                    | 200     | 11,882 B, `237572a8…`                                                                  |
| `/news/002-payload-selection/`                                                       | 200     | 14,265 B, `a8eb93c7…` — the same bytes the step-004 gate measured                      |
| `/news/003-vehicle-design/`                                                          | 200     | 11,380 B, `4f1277c1…`                                                                  |
| `/wiki/`                                                                             | 200     | 4,599 B, `821a2d84…` — unchanged since the gate's 18:58 measurement                    |
| `/about/`                                                                            | 200     | 8,815 B                                                                                |
| `/wiki/vehicle/`, `/wiki/vehicle/rh-01-pathfinder/`, `/wiki/area/`, `/wiki/project/` | **404** | 0 bytes — the wiki nodes are still fail-closed (004-C granted the STATE, not the flip) |
| `/news/does-not-exist/`                                                              | **404** | 0 bytes — no soft-404 that could serve an unpublished slug                             |
| `/sitemap.xml`                                                                       | **404** | none served (pre-existing site-wide; observation O-2 below)                            |
| `/robots.txt`                                                                        | 200     | 1,248 B                                                                                |

**Note on the local `dist/`.** The shared checkout's `website/dist/` is **not** the live surface and
was not used as evidence: it holds no `/news/004-launch-provider/` route (it predates the push) and
other profiles build into the same directory. The authoritative surface for a push-triggered deploy is
`origin/main`'s build — which is what the measurements above fetch.

### 2. The article page itself

| Check                    | Measured                                                                                                                                                                                                          |
| ------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Document title           | `The launch architecture, chosen. \| Red Horizon` — the writer's drafted title, no byline                                                                                                                         |
| `h1` / heading structure | one `h1`, `The launch architecture, chosen.`; **no `h2`**; kicker `Launch architecture 04 · Architecture selected`                                                                                                |
| Record-date line         | `Simulated record date · 11 March 2030` — exactly **1** occurrence, element class `article-date`                                                                                                                  |
| Machine-readable dates   | **0** ISO tokens, **0** `<time>` elements, **0** date-bearing meta tags; `description` = the 153-character summary                                                                                                |
| Canonical / OG           | no `<link rel="canonical">` (site built without `SITE_URL`, pre-existing and site-wide)                                                                                                                           |
| Figures / images         | **0** `<figure>`, **0** article images; the only `<img>` on the page is the site logo `/_astro/logo-bg-black.DZLOhNzm_Z1aNMaQ.webp`                                                                               |
| Anchors (full inventory) | nav and footer (`/#top`, `/news/`, `/wiki/`, `/#mission`, `/#timeline`, `/about/`) plus the article's own three links — see below                                                                                 |
| Article links            | `/#mission` (fiction disclosure), `/news/003-vehicle-design/` and `/news/001-project-announcement/` — all published, all 200; no external link, so no real institution's URL carries a name onto a public surface |
| Designation occurrences  | `Ariane 64`: **5** on the page (body ×4, summary ×1)                                                                                                                                                              |

### 3. The card on `/` and `/news/`

| Check                | `/news/`                                                                | `/`     |
| -------------------- | ----------------------------------------------------------------------- | ------- |
| Cards rendered       | 4 (001, 002, 003, 004)                                                  | 4       |
| 004 card link        | `/news/004-launch-provider/` — present                                  | present |
| 004 kicker           | `Launch architecture 04 · Architecture selected`                        | same    |
| 004 link label       | `Launch architecture briefing`                                          | same    |
| 004 media cell       | `news-placeholder placeholder-amber`, number `04`, **no `<img>`**       | same    |
| Record-date lines    | 4 — `12 October 2026`, `19 March 2027`, `13 July 2029`, `11 March 2030` | same    |
| Withheld-marker hits | 0                                                                       | 0       |

### 4. Withheld-marker scan

Scanned every served route above plus the whole released article file, by family, reporting hit counts
only (no withheld value is written into this record or into the repository):

| Family                                                                       | Result on the released article and every live route                                                                                                                                                                                                                                                |
| ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| The two landing-region coordinate markers (`gatedTextMarkers`, `guards.mjs`) | **0**                                                                                                                                                                                                                                                                                              |
| Withheld timeline paths (`docs/timeline/…`)                                  | **0**                                                                                                                                                                                                                                                                                              |
| The withheld scene file name (the per-file rule in `guards.mjs`)             | **0**                                                                                                                                                                                                                                                                                              |
| Institution / operating-company family                                       | **0** on the article and on `/`, `/news/`, 001, 003, `/wiki/`, `/about/`; 1 on `/news/002-payload-selection/`, pre-existing and already released (that article's own sources section inside its explicit "not partners" framing; the same 14,265 B / `a8eb93c7…` bytes the step-004 gate measured) |
| Launch-facility family                                                       | **0**                                                                                                                                                                                                                                                                                              |
| Withheld launch-day forms                                                    | **0**                                                                                                                                                                                                                                                                                              |
| Asteria Field / landing-design material                                      | 0 on this article (5 pre-existing hits on article 001, which released that material)                                                                                                                                                                                                               |

The guard module's own coverage at verdict time: `gatedDirectoryNames = ['timeline']`, one withheld
file (`docs/vehicles/pathfinder/contact-arm-scene.png`), and `gatedTextMarkers = ['18° 42',
'226° 14']`. Nothing in the released article matches any of them, and the guard carries **no** marker
covering the launch-vehicle designation it now releases — which is the required direction (ledger C5).

### 5. What this card did not measure, and why

- **No build, no guard CLI.** `scripts/check-dist.mjs` prunes offending files from `dist/` when it
  fails, so a release card must not run it; the build card's own run is the build evidence, and the
  live-origin comparison against that build is this card's strongest available measurement.
- **No commit, push or deploy.** The build card pushed; this card's verification is read-only.
- **`docs/SCENARIO.md`, `docs/timeline/` and the private timeline were read only.** No canon file was
  written, and no unreleased value is restated anywhere in this record.

---

## Conditions carried by the release decision — status

| # | Condition (source)                                                                                                                                                                                        | Status at this verdict                                                                                                                                                                                                                                                   |
| - | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 1 | Flip and record date in **one** change, no second divergent flip; the release decision exists before the push (`AGENTS.md`) — review §12.6                                                                | **SATISFIED.** `4d1795a` carries both frontmatter lines together; this card re-derived the flip diff from the released bytes and the approved hash reconstructs exactly                                                                                                  |
| 2 | **Ledger C5** — the build guard has no marker for the still-withheld families (institution / operator / facility names, launch-day forms) and must not gain one covering the released designation         | **NOT TRIGGERED and still open.** No guard file was edited by the release and the production build passed with the released designation present 5×; the missing coverage remains a defence-in-depth item owned by `mars-ai-simulator-dev`                                |
| 3 | **Order-sensitivity** — the article's negatives are time-bound by its record date; step 005 must carry a later record date — review §12.6                                                                 | **STANDS.** 002 (2027-03-19) and 003 (2029-07-13) both carry earlier record dates and state in the present tense that no launch vehicle has been named, so in-fiction they read as statements about their own dates. 2030-03-11 is a **lower bound** on step 005's value |
| 4 | **Hotspot** — the uncarded `docs/vehicle/` → `docs/vehicles/` split touches `website/scripts/guards.mjs`, `src/lib/assets.ts`, `src/features/news/media.ts`; stage only a card's own paths                | **STANDS, owner named.** Untracked/staged in this checkout, not part of `4d1795a`; see observation O-1 and O-2 below                                                                                                                                                     |
| 5 | The visuals candidate stays unplaced; the **RH-01** wiki leaf's publication state stays unrecorded (planner); the homepage roadmap follow-up adds the 004 link after the article publishes — review §12.6 | **STANDS.** No launcher media key exists; `/wiki/vehicle/rh-01-pathfinder/` 404; the roadmap card `t_116ca903` is running (observation O-3)                                                                                                                              |
| 6 | **C4** — the launch-vehicle wiki node's own flip is a separate release decision on that page's release card — ledger C4/C9                                                                                | **STANDS.** 004-C granted the STATE; measured `/wiki/vehicle/` 404, so no flip has occurred; the decision is this role's on that page's own card                                                                                                                         |
| 7 | **C10** — no byline anywhere, and the title is the writer's drafted title until a further recorded human answer                                                                                           | **SATISFIED.** Released frontmatter has no `author`; the title is `The launch architecture, chosen.`; this card changed neither                                                                                                                                          |

## Observations — recorded, none blocking this release

- **O-1 — the uncarded `docs/vehicle/` → `docs/vehicles/` split is live in this shared checkout, and a
  sibling card is publishing it right now.** `git status` at verdict time shows the 7 staged renames to
  `docs/vehicles/pathfinder/…`, 15 unstaged modifications (including `website/scripts/guards.mjs`,
  `src/lib/assets.ts`, `src/features/news/media.ts`, `test/guards.test.mjs`) and the untracked
  `docs/vehicles/ariane/ariane-64.md`. It is a hotspot for the files the step-004 cards touch, it is
  **not** part of this release, and card `t_206bee14` (`mars-ai-simulator-dev`) is `running` and owns
  the decision. Flagged as a hotspot on this card.
- **O-2 — the launcher dossier must not be published on step 004's scope.** Measured on
  `docs/vehicles/ariane/ariane-64.md` (untracked, 31,797 B, sha256 `3ef87d9d…`): it carries **1**
  operating-company/institution-family string, **1** agency-family string, **1** launch-facility
  string, **3** occurrences of a withheld launch-day form and **1** withheld `docs/timeline/` path
  (counts only; no value is restated here). Its own publication state is unrecorded, and 004-B's
  recorded constraint names a **dossier** among the surfaces where no institution name may appear, so
  the fail-closed default binds. **Owner:** `mars-ai-simulator-dev` on `t_206bee14` — which is scoped
  to block rather than publish when the release state is unrecorded — with `mars-ai-simulator-editor`
  or the human story owner as the deciding authority if a dossier publication is ever wanted.
- **O-3 — the homepage roadmap panel is being updated by a running sibling card.** `t_116ca903`
  (`mars-ai-simulator-writer`, running) adds the step-004 link and decides the current flag in
  `src/features/progress/steps.ts`, whose source comment says to add a step's link once its article is
  published. The panel's strings were true before the flip; this release is what makes the update due.
  It is a follow-up, not a blocker, and it can move `/` after this record's measurement (measured `/`
  18,846 B, `4c16d7f1…`, 19:47 CEST).
- **O-4 — the shared checkout moved under this record while it was being written, and it is a
  measured movement, not a divergence in the release.** Timestamps: the live measurements above were
  taken at ~19:47–19:52 CEST with `HEAD` = `origin/main` = `4d1795a` (`rev-list origin/main..HEAD` = 0).
  At 19:50 CEST `git fetch origin` left `origin/main` at `4d1795a`. At **19:51:47 CEST** the same
  checkout reported `HEAD` = `07c0502` with **2** local commits ahead of `origin/main`
  (`35575e7`, `07c0502`, both authored by `mars-ai-simulator-dev` on `t_206bee14`) — a sibling card
  committing in the shared tree, not yet pushed. Neither commit touches the released article:
  `website/news/004-launch-provider.mdx` appears in **0** of the two commits' file lists and still
  hashes `7da71fec…`, `website/scripts/guards.mjs` still hashes `3c95f223…`, and the live origin still
  serves the released page byte-identically (`49e0227d…`, 9,730 B) after the movement. What the two
  commits do carry is `.agents/skills/site-deployment/SKILL.md` (three deploy lessons) and the nine
  step-004 working papers, published as authored. Effect on this record: **none** — the article bytes
  are the release's identity and they did not move; the release scope is unchanged.
- **O-5 — no canonical link and no sitemap on any live route** (the site is built without
  `SITE_URL`; `/sitemap.xml` 404). Pre-existing for 001–003, not caused by this release and not a
  release condition. **Owner:** `mars-ai-simulator-seo`.
- **O-6 — the released page's own media treatment is the amber placeholder.** The first published item
  with no media key renders a class-list placeholder cell; the build card repaired the plate-fill
  suite's matcher rather than loosening an assertion, and the repair is in `4d1795a`.
- **O-7 — this record is working material and reaches `origin/main` only through a card with push
  authority.** This card commits and pushes nothing, so `.agents/work/releases/004-launch-provider.md`
  is untracked. `t_206bee14` (`mars-ai-simulator-dev`) is scoped to publish it as authored when it is
  present, and its two commits at 19:51 CEST did **not** include it — the file was written after them.
  Any later publication pass must publish it as authored: its sha256 is cited in this card's handoff
  and its tables are hand-rewritten, so the pre-commit hook would reflow the staged bytes
  (`git commit --no-verify` is the recorded recipe for `.agents/work/` papers whose hashes are
  cited elsewhere).

---

## Inputs this release rests on

- Brief: `.agents/work/briefs/004-launch-provider.md`; canon proposal: `.agents/work/briefs/004-launch-provider-canon.md`
- Research: `.agents/work/sources/004-launch-provider.md`
- SEO: `.agents/work/seo/004-launch-provider.md`
- Assets manifest: `.agents/work/assets/004-launch-provider/assets.md` (candidate only; nothing placed)
- Continuity: `.agents/work/continuity/004-launch-provider.md` — **`continuity clear`** (R2), on the same article bytes; gate ledger companion `.agents/work/continuity/004-launch-provider-gates.md`
- Editorial gate and release decision: `.agents/work/reviews/004-launch-provider.md` — **`approved`**, §12.5; gate ledger `.agents/work/reviews/004-launch-provider-gates.md` §R3.1–§R3.7
- Canon cited: `docs/SCENARIO.md` (`:22`, `:124`, `:173`, `:187`, `:188`, `:193`, `:230`, `:279`, `:281`); `docs/vehicles/ariane/ariane-64.md` (dossier, not published)
- Build and deploy: card `t_de6e9055` (commit `4d1795a`, push `ad8f5ac..4d1795a`, CI run `35254536487`, Cloudflare Workers Build success)
- Guard module: `website/scripts/guards.mjs` — `3c95f223…`
- Live origin: `https://mars-ai-simulation.janpolacek.workers.dev` (2026-09-17 ~19:47–19:52 CEST)

## Revision history of this record

| Revision          | SHA-256                                                      | Author                                    | What moved                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------- | ------------------------------------------------------------ | ----------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 — this revision | recorded on card `t_1105aa0d` (self-embedding is impossible) | `mars-ai-simulator-editor` (`t_1105aa0d`) | status `released`; the release decision and the exact public scope; gates 004-A…004-E with their required wordings, recorded answers and applied scope; the containment checklist resolved line by line; the media-key outcome (none); the applied flip diff with the reconstruction proof; released hashes; the record-date verification on the live surface; the six-rule review (C2); the live-origin, article-page and card tables; the withheld-marker scan; conditions C1–C7 statused; observations O-1–O-7 |

---

_This record is working material, not canon. Canon lives in `docs/SCENARIO.md`. The continuity verdict
and the editorial verdict are gate outcomes; the release decision recorded here and on card
`t_35061463` is what licensed the flip, and the push of that flip by `t_de6e9055` is what published
the article._
