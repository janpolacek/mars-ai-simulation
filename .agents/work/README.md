# Working papers — current pipeline cycle

Per-article working papers for the active card cycle live here: `briefs/`,
`sources/`, `assets/<slug>/`, `drafts/`, `seo/`, `reviews/`, `continuity/`,
`releases/`, plus the live image registry `image-index.md` (see
`.agents/skills/image-index/SKILL.md`). Papers are pipeline working material,
not documentation — the docs map is `docs/README.md`, and the definitive task
record is the Hermes kanban card for each piece of work.

## Archive of the completed cycle (2026-09-20)

The first full cycle — articles 001–011, the milestone articles, the four wiki
pages, and the homepage current-state work (2026-09-16 → 2026-09-20) — is
archived:

- Portable snapshot: `~/mars-ai-simulator-work-2026-09-20.tar.gz` (complete
  `.agents/work` tree as it stood at cleanup, including untracked files).
- Git history: the papers are tracked in git; the pre-cleanup tree is tagged
  `archive/work-cycle-2026-09-20`, and the cleanup commit removed them from the
  working tree.
- The knowledge they carried is already reflected in the published surface
  (`website/news/`, `website/wiki/`), the dossiers in `docs/`, and each card's
  decision record on the kanban board.

New papers for the next cycle start fresh in the directories above.
